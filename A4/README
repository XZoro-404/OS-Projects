# README.TXT
By: Ben Richmond and Christian Black

# Compile and Run instructions
To compile this program you run the following command on your linux machine.
'gcc main.c' and to run the program run './a.out {input file}'

# Limitations
There are no known limitations to this program.

# Features
The features that are included in this program are: printing the amount of page faults for each page replacement
    algorithm, create two files that contain the physical addresses, checks if a page already resides on the ram then
    will allocate the page to a frame if a page fault occurs, and if there are no frames available remove a frame using
    FIFO or LRU to remove a page.